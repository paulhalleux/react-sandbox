{
  "$id": "https://example.com/string.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "String",
  "description": "A string schema",
  "type": "string",
  "minLength": 1,
  "maxLength": 50,
  "default": "",
  "$comment": "A string is a sequence of characters.\n The default value is an empty string.\n The minimum length is 1 and the maximum length is 50.",
  "examples": [
    "Example string"
  ]
}
